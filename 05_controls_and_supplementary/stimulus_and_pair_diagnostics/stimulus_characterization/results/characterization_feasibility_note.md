# Stimulus Characterization Feasibility

Available local annotations support low-level image statistics and clusters in that statistics space. No OCR/text detections, object counts, person/animal counts, or scene labels were present in the paper cache, so those fields are not asserted here. They can be added as a separate annotation table keyed by `stimulus_set` and `image`.
