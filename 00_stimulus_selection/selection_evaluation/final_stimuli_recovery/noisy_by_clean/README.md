# Final Stimuli Noisy-By-Clean Recovery

This directory applies the shared noisy-by-clean recovery method to the frozen
final selected stimulus sets.

Scripts:

- `90_run_recovery.sh`: local compute-and-plot convenience runner.
- `91_submit_recovery_slurm.sh`: SLURM submission helper.

Shared Python implementations live in
`../../code/noisy_by_clean/`.
