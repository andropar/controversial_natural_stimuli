Superseded teacher/student artifacts archived on 2026-06-14.

This archive contains response-space teacher/student runs, empirical-only RDM
diagnostics, smoke/tiny diagnostics, and old plotting/run wrappers that were
replaced by the full-grid independent-refit RDM-score analysis.

The active method is:

- `code/compute_teacher_student_independent_refit_rdm_score.py`
- `code/plot_teacher_student_rdm_score_curves.py`
- `results/*_teacher_student_independent_refit_1k_rdm_score_rdm`

The base scripts `compute_teacher_student_recovery.py` and
`compute_teacher_student_independent_refit.py` remain in `code/` because the
active RDM-score script imports utility functions from them.
