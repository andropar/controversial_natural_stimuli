hydra:
  run:
    dir: /u/rothj/cstims/outputs/final_cstims_v2_full/training_objective/method-raw_plus_all_encodings/20251222_175721
  sweep:
    dir: ${paths.output_base}/multirun/${model_set_name}/method-${method_name}/metric-${metric}_corr-${corr_type}_aggwithin-${aggregation_within}_aggacross-${aggregation_across}/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.run.dir=/u/rothj/cstims/outputs/final_cstims_v2_full/training_objective/method-raw_plus_all_encodings/20251222_175721
    - hydra.mode=RUN
    task:
    - paths=raven
    - corr_type=correlation
    - use_analytical=true
    - model_set=training_objective
    - method=raw_plus_all_encodings
    - metric=cosine
    - aggregation_within=mean
    - aggregation_across=min
    - max_ram_gb=300
    - target_size=100
    - batch_size=2500
    - refinement.max_passes=10
    - refinement.min_replacements=0
    - image_filter.enabled=true
  job:
    name: select_controversial_stimuli
    chdir: false
    override_dirname: aggregation_across=min,aggregation_within=mean,batch_size=2500,corr_type=correlation,image_filter.enabled=true,max_ram_gb=300,method=raw_plus_all_encodings,metric=cosine,model_set=training_objective,paths=raven,refinement.max_passes=10,refinement.min_replacements=0,target_size=100,use_analytical=true
    id: ???
    num: ???
    config_name: config
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.3'
    cwd: /tmp
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /raven/u/rothj/cstims/conf
      schema: file
      provider: main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /u/rothj/cstims/outputs/final_cstims_v2_full/training_objective/method-raw_plus_all_encodings/20251222_175721
    choices:
      method: raw_plus_all_encodings
      model_set: training_objective
      paths: raven
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: default
      hydra/hydra_logging: default
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
